[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = tests
addopts =
    --reuse-db
    --cov=apps
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=50
    -v
